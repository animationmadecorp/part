export const metadata = {
  title: "Animation Made — Ton talent, ta trajectoire",
  description: "Un accompagnement personnel pour avancer dans l’animation.",
  robots: { index: true, follow: true },
};

export default function Layout({ children }) { return <div className="am-new">{children}</div>; }
