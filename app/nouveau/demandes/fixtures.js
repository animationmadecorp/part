// Kept as a compatibility seam for the correction studio. Real dossiers are
// loaded by convex/adminRequests.js; no sample person or fake file is exposed.
export const requestFixtures = [];

export function getRequestContext() { return null; }
